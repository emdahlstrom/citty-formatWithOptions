# citty-formatwithoptions

To install dependencies:

```bash
bun install
```

To run:

```bash
bun start
```

# Expected error

> SyntaxError: Import named 'formatWithOptions' not found in module 'node:util'.
> error: script "start" exited with code 1 (SIGHUP)